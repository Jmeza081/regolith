# Repomix snapshot (regenerate with `repomix`)
repomix-output.xml

# Android / Gradle build output and per-machine config
.gradle/
build/
.kotlin/
.cxx/
captures/
local.properties
*.apk
*.aab

# IDE
.idea/
*.iml

# macOS
.DS_Store

# argent tool artifacts (screen recordings, flows scratch)
.argent/

# Mac app: libvlc fetched by :desktop:fetchVlc (~82 MB), bundled into the .app, never committed
desktop/vlc-bundle/

# Phone screenshot baselines for visual regression checks (argent); local only, never committed
.baselines/
