<?xml version="1.0" encoding="utf-8"?>
<!--
  The manifest is the app's static declaration to the OS: which "pages"
  (Activities) exist, which one is the entry point, and what the app is
  allowed to do. Compose apps have one Activity and draw every screen in it.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- The app lock: fingerprint, face, or the phone's own screen lock. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Android 17 gates every connection to a LAN address behind this
         runtime permission for apps targeting SDK 37. Without it, SMB
         connects silently time out. Requested on the Add Server screen. -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />
    <!-- The library scan runs as a foreground job of type dataSync so the
         OS keeps it alive when the app is backgrounded ("Run in the
         background"). The notification needs POST_NOTIFICATIONS on 13+;
         without it the job still runs, the notification is just hidden. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".RegolithApp"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Regolith.Splash">

        <!-- WorkManager normally initialises itself through this provider.
             Removing it lets RegolithApp supply a Hilt-aware configuration
             so workers can @Inject repositories. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.Regolith.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
              "Open with Regolith". Android offers an app in the chooser for a
              file only if it declares it can VIEW that file's type, and the
              type arrives two different ways: a content:// URI from a picker
              or a chooser, where the provider states the MIME type, and a
              file:// or http(s):// URI with an extension and often no type at
              all. Both are declared, which is why the same action appears
              twice with different data.
            -->
            <intent-filter android:label="@string/open_with_regolith">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="video/*" />
                <data android:scheme="content" />
                <data android:scheme="file" />
            </intent-filter>
            <intent-filter android:label="@string/open_with_regolith">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <!-- The containers Regolith actually plays; a wildcard here
                     would put it in the chooser for documents and archives. -->
                <data android:pathPattern=".*\\.mkv" />
                <data android:pathPattern=".*\\.mp4" />
                <data android:pathPattern=".*\\.m4v" />
                <data android:pathPattern=".*\\.mov" />
                <data android:pathPattern=".*\\.webm" />
                <data android:pathPattern=".*\\.avi" />
                <data android:pathPattern=".*\\.ts" />
                <data android:pathPattern=".*\\.m2ts" />
            </intent-filter>
        </activity>
    </application>
</manifest>
